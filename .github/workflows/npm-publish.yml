name: Node.js Package

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest 
    permissions: 
      contents: read
      packages: write 
    steps:
      - uses: actions/checkout@v2
      # Setup .npmrc file to publish to GitHub Packages
      - uses: actions/setup-node@v2
        with:
          node-version: '12.x'
          registry-url: 'https://npm.pkg.github.com'
          # Defaults to the user or organization that owns the workflow file
          scope: '@aj-7885'
      - run: npm install
      - run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          
#   publish-gpr:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v2
#       - uses: actions/setup-node@v1
#         with:
#           node-version: 14
#           registry-url: https://npm.pkg.github.com/
#           scope: '@aj-7885'
#       - run: yarn install
#       - run: yarn publish
#         env:
#           NODE_AUTH_TOKEN: ${{secrets.GITHUB_TOKEN}}

# - name: Publish to npm
#   # You may pin to the exact commit or the version.
#   # uses: pascalgn/npm-publish-action@07e88a42392c8c117aa9582b22ad2b2e70248ea5
#   uses: pascalgn/npm-publish-action@1.3.6
#   with:
#     # The pattern that matches version update commits.
#     commit_pattern: # optional
#     # The name of the tag that you want to create for the version.
#     tag_name: # optional
#     # The message of the tag that you want to create for the version.
#     tag_message: # optional
#     # Custom workspace directory that contains the package.json file.
#     workspace: # optional
#     # Custom publish command.
#     publish_command: # optional
#     # Publish command arguments.
#     publish_args: # optional
